import { ViewStyle } from 'react-native';
interface AvatarProps {
    name: string;
    size?: number;
    uri?: string;
    style?: ViewStyle;
}
export declare function Avatar({ name, size, uri, style }: AvatarProps): import("react/jsx-runtime").JSX.Element;
export {};
